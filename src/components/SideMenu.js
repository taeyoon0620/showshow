import React from 'react';
import { Box, Typography } from '@mui/material';

const SideMenu = () => (
    <Box position="sticky" top={0}>
        <Typography>사용자 스크롤에 따라 자동으로 움직이는 메뉴목록</Typography>
    </Box>
);

export default SideMenu;